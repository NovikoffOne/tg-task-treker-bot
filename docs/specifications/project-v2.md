# Спецификация проекта v2: Advanced Telegram Task Tracker Bot

## Общее описание

Продвинутый телеграм-бот для управления проектами и задачами с поддержкой пространств, досок, колонок, проектов и синхронизации данных. Аналог Trello/Jira для Telegram.

## Функциональные требования

### 1. Управление пространствами (Workspaces)

#### 1.1 Создание пространства
- **Команда:** `/newworkspace <название>` или `/newspace <название>`
- **Пример:** `/newworkspace Работа`
- **Процесс:**
  1. Пользователь отправляет команду с названием
  2. Бот проверяет уникальность названия для пользователя
  3. Создает пространство
  4. Показывает подтверждение
- **Результат:** Пространство создано, пользователь может создавать в нем доски

#### 1.2 Просмотр пространств
- **Команда:** `/workspaces` или `/spaces`
- **Формат отображения:**
  ```
  📁 Ваши пространства:
  
  🏢 Работа (3 доски, 15 задач)
  🏠 Дом (2 доски, 8 задач)
  🏃 Спорт (1 доска, 3 задачи)
  ```

#### 1.3 Удаление пространства
- **Команда:** `/delworkspace <название>` или `/delspace <название>`
- **Подтверждение:** Запрос подтверждения (удаляются все доски и задачи)
- **Результат:** Пространство и все данные удалены

#### 1.4 Переименование пространства
- **Команда:** `/renameworkspace <старое_название> <новое_название>`
- **Результат:** Название изменено во всех связанных данных

### 2. Управление досками (Boards)

#### 2.1 Создание доски
- **Команда:** `/newboard <название>` (в текущем пространстве)
- **Процесс:**
  1. Пользователь выбирает пространство (если несколько)
  2. Отправляет команду создания доски
  3. Бот создает доску с дефолтными колонками или запрашивает колонки
- **Пример:** `/newboard Дизайн` в пространстве "Работа"

#### 2.2 Просмотр досок
- **Команда:** `/boards` - показать доски текущего пространства
- **Формат:**
  ```
  📋 Доски в пространстве "Работа":
  
  🎨 Дизайн (3 колонки, 12 задач)
  💻 Разработка (4 колонки, 25 задач)
  🧪 Тестирование (3 колонки, 8 задач)
  ```

#### 2.3 Удаление доски
- **Команда:** `/delboard <название>`
- **Подтверждение:** Запрос подтверждения
- **Результат:** Доска и все задачи удалены

#### 2.4 Переименование доски
- **Команда:** `/renameboard <старое> <новое>`

### 3. Управление колонками (Columns)

#### 3.1 Создание колонки
- **Команда:** `/addcolumn <доска> <название>`
- **Пример:** `/addcolumn Дизайн "В работе"`
- **Процесс:**
  1. Бот проверяет существование доски
  2. Проверяет уникальность названия колонки в доске
  3. Создает колонку в конце списка
- **Результат:** Колонка добавлена

#### 3.2 Просмотр колонок
- **Команда:** `/columns <доска>` или автоматически при просмотре доски
- **Формат:**
  ```
  📌 Колонки доски "Дизайн":
  
  1. Очередь (5 задач)
  2. План на неделю (3 задачи)
  3. В работе (2 задачи)
  4. На утверждении (1 задача)
  5. Правки (0 задач)
  6. Готово (8 задач)
  ```

#### 3.3 Удаление колонки
- **Команда:** `/delcolumn <доска> <колонка>`
- **Важно:** При удалении колонки задачи перемещаются в первую колонку или удаляются (на выбор)

#### 3.4 Изменение порядка колонок
- **Команда:** `/movecolumn <доска> <колонка> <позиция>`
- **Альтернатива:** Inline-кнопки для перемещения

### 4. Управление проектами (Projects)

#### 4.1 Создание проекта
- **Команда:** `/newproject <id> <название>`
- **Пример:** `/newproject 5010 "Nano Banana Ai"`
- **Процесс:**
  1. Бот проверяет уникальность ID проекта в пространстве
  2. Создает проект
  3. Автоматически создает задачи проекта на всех досках пространства:
     - "5010 Nano Banana Ai Дизайн" → доска "Дизайн"
     - "5010 Nano Banana Ai Разработка" → доска "Разработка"
     - "5010 Nano Banana Ai Тестирование" → доска "Тестирование"
  4. Все задачи помещаются в первую колонку (Очередь)
  5. Все задачи связываются с проектом (project_id)
- **Результат:** Проект создан, задачи созданы на досках

#### 4.2 Просмотр проекта
- **Команда:** `/project <id>` или `/projectinfo <id>`
- **Формат:**
  ```
  📊 Проект: 5010 Nano Banana Ai
  
  📈 Дашборд: Разработка (текущий этап)
  
  Этапы:
  ✅ Подготовка
  ✅ Дизайн
  🔄 Разработка ← текущий
  ⏳ Тестирование
  ⏳ На отправку
  ⏳ Модерация
  ⏳ Опубликовано
  
  📋 Задачи проекта:
  • 5010 Nano Banana Ai Дизайн [Готово]
  • 5010 Nano Banana Ai Разработка [В работе]
  • 5010 Nano Banana Ai Тестирование [Очередь]
  ```

#### 4.3 Дашборд проекта
- **Команда:** `/projectdashboard <id>` или `/dashboard <id>`
- **Этапы дашборда:**
  - `preparation` - Подготовка
  - `design` - Дизайн
  - `development` - Разработка
  - `testing` - Тестирование
  - `submission` - На отправку
  - `moderation` - Модерация
  - `rejected` - Реджект
  - `published` - Опубликовано
- **Автоматическое определение этапа:**
  - Если задача "Дизайн" в колонке "Готово" → этап "Дизайн" завершен
  - Если задача "Разработка" в колонке "В работе" → текущий этап "Разработка"

#### 4.4 Удаление проекта
- **Команда:** `/delproject <id>`
- **Важно:** Задачи проекта остаются, но теряют связь с проектом (project_id = NULL)

### 5. Управление задачами (Tasks)

#### 5.1 Создание задачи
- **Команда:** `/newtask` или кнопка "➕ Новая задача"
- **Процесс:**
  1. Выбор доски (если несколько)
  2. Выбор колонки
  3. Ввод названия задачи
  4. Ввод описания (опционально)
  5. Привязка к проекту (опционально)
- **Результат:** Задача создана в выбранной колонке

#### 5.2 Просмотр задачи
- **Команда:** `/task <id>` или клик по задаче
- **Формат:**
  ```
  📋 Задача #123: 5010 Nano Banana Ai Дизайн
  
  📁 Проект: 5010 Nano Banana Ai
  📊 Доска: Дизайн
  📌 Колонка: В работе
  🔴 Приоритет: Высокий
  
  Описание: Дизайн интерфейса приложения
  
  🏷 Метки: #дизайн #ui #mobile
  
  📎 Поля:
  • Figma: https://figma.com/...
  • Git: https://github.com/...
  
  📝 Подзадачи:
  ✅ Создать макеты
  🔄 Подготовить иконки
  ⏳ Адаптация под iOS
  
  Создано: 25.01.2025 14:30
  Обновлено: 26.01.2025 10:15
  ```

#### 5.3 Перемещение задачи
- **Команда:** `/movetask <id> <колонка>` или drag&drop через кнопки
- **Процесс:**
  1. Пользователь выбирает задачу
  2. Выбирает целевую колонку
  3. Бот обновляет column_id и position
  4. Если задача проекта → обновляется дашборд проекта
- **Результат:** Задача перемещена

#### 5.4 Редактирование задачи
- **Команда:** `/edittask <id>` или кнопка "✏️ Редактировать"
- **Можно изменить:**
  - Название
  - Описание
  - Приоритет
  - Метки
  - Поля

#### 5.5 Удаление задачи
- **Команда:** `/deltask <id>` или кнопка "🗑 Удалить"
- **Подтверждение:** Запрос подтверждения
- **Результат:** Задача удалена

### 6. Подзадачи (Subtasks)

#### 6.1 Создание подзадачи
- **Команда:** `/newsubtask <parent_id> <название>`
- **Процесс:**
  1. Указывается родительская задача
  2. Создается подзадача с parent_task_id
  3. Подзадача наследует колонку родителя
- **Результат:** Подзадача создана

#### 6.2 Просмотр подзадач
- Отображаются в карточке родительской задачи
- Показывается прогресс выполнения (X из Y выполнено)

#### 6.3 Управление подзадачами
- Отметка выполнения
- Редактирование
- Удаление

### 7. Приоритеты задач

#### 7.1 Установка приоритета
- **Команда:** `/priority <id> <уровень>`
- **Уровни:**
  - `low` или `0` - Низкий 🟢
  - `medium` или `1` - Средний 🟡
  - `high` или `2` - Высокий 🟠
  - `critical` или `3` - Критический 🔴
- **Альтернатива:** Inline-кнопки для быстрого выбора

#### 7.2 Фильтрация по приоритету
- **Команда:** `/tasks priority <уровень>`
- Показывает задачи с указанным приоритетом

### 8. Метки (Tags)

#### 8.1 Создание метки
- **Команда:** `/newtag <название> [цвет]`
- **Пример:** `/newtag дизайн #FF5733`
- **Процесс:**
  1. Метка создается на уровне пространства
  2. Можно указать цвет (HEX) или использовать дефолтный
  3. Метка доступна для всех задач пространства

#### 8.2 Добавление метки к задаче
- **Команда:** `/addtag <task_id> <tag_name>`
- **Альтернатива:** Inline-кнопки для выбора меток
- **Результат:** Метка добавлена к задаче

#### 8.3 Удаление метки с задачи
- **Команда:** `/deltag <task_id> <tag_name>`

#### 8.4 Фильтрация по меткам
- **Команда:** `/tasks tag <tag_name>`
- Показывает все задачи с указанной меткой

### 9. Динамические поля (Custom Fields)

#### 9.1 Создание поля
- **Команда:** `/newfield <название> <тип>`
- **Типы полей:**
  - `text` - Текст
  - `url` - Ссылка
  - `number` - Число
  - `date` - Дата
  - `select` - Выбор из списка
- **Пример:** `/newfield Figma url`
- **Результат:** Поле создано на уровне пространства

#### 9.2 Добавление значения поля к задаче
- **Команда:** `/addfield <task_id> <field_name> <value>`
- **Пример:** `/addfield 123 Figma https://figma.com/file/...`
- **Процесс:**
  1. Бот проверяет существование поля
  2. Валидирует значение по типу поля
  3. Добавляет значение к задаче
  4. **Если задача проекта → синхронизирует поле со всеми задачами проекта**
- **Результат:** Поле добавлено, синхронизировано (если проект)

#### 9.3 Синхронизация полей проекта
- **Автоматическая синхронизация:**
  - При добавлении поля к задаче проекта
  - Поле автоматически добавляется ко всем задачам проекта
  - При изменении поля в одной задаче → обновляется во всех задачах проекта
- **Управление синхронизацией:**
  - `/syncfield <project_id> <field_name> enable` - включить синхронизацию
  - `/syncfield <project_id> <field_name> disable` - отключить синхронизацию

#### 9.4 Просмотр полей задачи
- Поля отображаются в карточке задачи
- Показывается тип поля и значение

#### 9.5 Удаление поля
- **Команда:** `/delfield <task_id> <field_name>`
- Если поле синхронизировано → удаляется из всех задач проекта

### 10. Статистика

#### 10.1 Общая статистика
- **Команда:** `/stats`
- **Формат:**
  ```
  📊 Общая статистика
  
  📁 Пространств: 3
  📋 Досок: 8
  📝 Задач: 45
  ✅ Выполнено: 12
  🔄 В работе: 18
  ⏳ В очереди: 15
  
  📈 Проектов: 5
  🎯 Активных проектов: 3
  ```

#### 10.2 Статистика проекта
- **Команда:** `/statsproject <id>` или `/stats <id>`
- **Формат:**
  ```
  📊 Статистика проекта: 5010 Nano Banana Ai
  
  Прогресс: 60% (3 из 5 этапов)
  
  Этапы:
  ✅ Подготовка
  ✅ Дизайн
  🔄 Разработка (60% готово)
  ⏳ Тестирование
  ⏳ На отправку
  
  Задачи:
  Всего: 8
  Выполнено: 3
  В работе: 2
  В очереди: 3
  ```

#### 10.3 Статистика доски
- **Команда:** `/statsboard <название>`
- Показывает распределение задач по колонкам, приоритетам, меткам

### 11. Визуализация досок

#### 11.1 Текстовое представление доски
- **Команда:** `/board <название>` или `/boardview <название>`
- **Формат:**
  ```
  📋 Доска: Дизайн
  
  ┌─────────────────┬─────────────────┬─────────────────┐
  │    Очередь      │    В работе     │     Готово      │
  ├─────────────────┼─────────────────┼─────────────────┤
  │ 🔴 #1 Task1     │ 🟠 #2 Task2     │ ✅ #3 Task3     │
  │    [High]       │    [High]       │    [Low]        │
  │    #design      │    #ui          │    #mobile      │
  │                 │                 │                 │
  │ 🟡 #4 Task4     │                 │ ✅ #5 Task5     │
  │    [Medium]     │                 │    [Low]        │
  │    #mobile      │                 │                 │
  └─────────────────┴─────────────────┴─────────────────┘
  
  Всего задач: 5
  В очереди: 2 | В работе: 1 | Готово: 2
  ```

#### 11.2 Компактное представление
- **Команда:** `/board <название> compact`
- Более компактный формат для больших досок

### 12. Управление задачами (расширенное)

#### 12.1 Массовые операции
- Выбор нескольких задач через inline-кнопки
- Массовое перемещение
- Массовое добавление меток
- Массовое изменение приоритета

#### 12.2 Поиск задач
- **Команда:** `/search <запрос>`
- Поиск по названию, описанию, меткам
- Фильтры: по проекту, доске, приоритету, метке

#### 12.3 Фильтрация
- По проекту: `/tasks project <id>`
- По доске: `/tasks board <название>`
- По колонке: `/tasks column <доска> <колонка>`
- По приоритету: `/tasks priority <уровень>`
- По метке: `/tasks tag <название>`
- Комбинированные фильтры: `/tasks project 5010 priority high`

## Интерфейс и UX

### Главное меню
```
[📁 Пространства] [📋 Доски]
[📊 Проекты]     [📝 Задачи]
[📈 Статистика]  [❓ Помощь]
```

### Inline-кнопки для задач
- `✅ Выполнено` - переместить в "Готово"
- `✏️ Редактировать` - редактировать задачу
- `🗑 Удалить` - удалить задачу
- `📎 Добавить поле` - добавить динамическое поле
- `🏷 Метки` - управление метками
- `🔴 Приоритет` - изменить приоритет
- `📋 Подзадачи` - управление подзадачами

### Навигация
- Кнопки для перехода между пространствами, досками, проектами
- Хлебные крошки для понимания текущего местоположения
- Быстрый доступ к часто используемым ресурсам

## Команды бота (полный список)

### Пространства
- `/workspaces` - Список пространств
- `/newworkspace <name>` - Создать пространство
- `/delworkspace <name>` - Удалить пространство
- `/renameworkspace <old> <new>` - Переименовать

### Доски
- `/boards` - Список досок
- `/newboard <name>` - Создать доску
- `/delboard <name>` - Удалить доску
- `/renameboard <old> <new>` - Переименовать
- `/board <name>` - Показать доску

### Колонки
- `/columns <board>` - Список колонок
- `/addcolumn <board> <name>` - Добавить колонку
- `/delcolumn <board> <name>` - Удалить колонку
- `/movecolumn <board> <column> <position>` - Переместить колонку

### Проекты
- `/projects` - Список проектов
- `/newproject <id> <name>` - Создать проект
- `/project <id>` - Показать проект
- `/projectdashboard <id>` - Показать дашборд
- `/delproject <id>` - Удалить проект

### Задачи
- `/newtask` - Создать задачу
- `/task <id>` - Показать задачу
- `/edittask <id>` - Редактировать задачу
- `/deltask <id>` - Удалить задачу
- `/movetask <id> <column>` - Переместить задачу
- `/newsubtask <parent_id> <name>` - Создать подзадачу

### Приоритеты
- `/priority <id> <level>` - Установить приоритет
- `/tasks priority <level>` - Фильтр по приоритету

### Метки
- `/newtag <name> [color]` - Создать метку
- `/addtag <task_id> <tag>` - Добавить метку к задаче
- `/deltag <task_id> <tag>` - Удалить метку
- `/tasks tag <tag>` - Фильтр по метке

### Поля
- `/newfield <name> <type>` - Создать поле
- `/addfield <task_id> <field> <value>` - Добавить поле к задаче
- `/delfield <task_id> <field>` - Удалить поле
- `/syncfield <project_id> <field> <enable|disable>` - Управление синхронизацией

### Статистика
- `/stats` - Общая статистика
- `/statsproject <id>` - Статистика проекта
- `/statsboard <name>` - Статистика доски

### Поиск и фильтры
- `/search <query>` - Поиск задач
- `/tasks project <id>` - Фильтр по проекту
- `/tasks board <name>` - Фильтр по доске
- `/tasks column <board> <column>` - Фильтр по колонке
- `/tasks priority <level>` - Фильтр по приоритету
- `/tasks tag <name>` - Фильтр по метке

## Обработка ошибок

### Валидация
- Названия: минимум 2 символа, максимум 100
- Описания: максимум 2000 символов
- ID проектов: уникальность в пространстве
- URL полей: валидация формата
- Даты: валидация формата

### Сообщения об ошибках
- "Пространство с таким названием уже существует"
- "Доска не найдена"
- "Проект с таким ID уже существует"
- "Задача не найдена"
- "У вас нет прав на эту операцию"
- "Некорректный формат URL"

## Критерии готовности

1. ✅ Все команды работают корректно
2. ✅ Пространства, доски, колонки функционируют
3. ✅ Проекты создаются с автоматическими задачами
4. ✅ Дашборд проекта работает
5. ✅ Синхронизация полей работает
6. ✅ Приоритеты и метки работают
7. ✅ Статистика отображается корректно
8. ✅ Визуализация досок работает
9. ✅ Поиск и фильтры работают
10. ✅ Обработка ошибок реализована
11. ✅ База данных стабильна
12. ✅ Бот не падает при некорректных данных

