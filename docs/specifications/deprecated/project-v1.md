# Спецификация проекта: Telegram Task Tracker Bot

> **⚠️ УСТАРЕВШИЙ ДОКУМЕНТ**  
> Это спецификация базовой версии MVP (v1.0). Проект был значительно расширен до версии 2.0 с полным функционалом управления проектами.  
> **Актуальная спецификация:** См. [project-v2.md](../project-v2.md)  
> **Дата создания:** 2025-01-25  
> **Статус:** Устарел, сохранен для истории

## Общее описание

Базовый телеграм-бот для управления задачами (таск-трекер). Позволяет пользователям создавать, просматривать, обновлять и удалять задачи через простой интерфейс Telegram.

**Цель MVP:** Создать рабочий прототип таск-трекера для тестирования концепции. В дальнейшем будет интегрирован AI-агент для автоматического управления задачами.

## Функциональные требования

### 1. Управление задачами

#### 1.1 Создание задачи
- **Команда:** `/newtask` или кнопка "➕ Создать задачу"
- **Процесс:**
  1. Пользователь отправляет `/newtask`
  2. Бот запрашивает: "Введите название задачи:"
  3. Пользователь вводит название (обязательно, минимум 3 символа)
  4. Бот запрашивает: "Введите описание задачи (или отправьте /skip для пропуска):"
  5. Пользователь вводит описание или `/skip`
  6. Бот создает задачу и показывает подтверждение
- **Результат:** Задача создается со статусом "В работе" и текущей датой

#### 1.2 Просмотр задач
- **Команда:** `/tasks` - все задачи пользователя
- **Команда:** `/active` - только активные задачи (статус "В работе")
- **Команда:** `/done` - только завершенные задачи
- **Формат отображения:**
  ```
  📋 Задача #1
  Название: [название]
  Описание: [описание или "Без описания"]
  Статус: ⏳ В работе / ✅ Выполнено
  Создано: [дата в формате ДД.ММ.ГГГГ ЧЧ:ММ]
  ```
- **Пагинация:** По 5 задач на страницу с кнопками "⬅️ Назад" и "➡️ Вперед"

#### 1.3 Изменение статуса задачи
- **Команда:** `/done_task <task_id>` - отметить задачу как выполненную
- **Альтернатива:** Inline-кнопка "✅ Выполнено" в сообщении с задачей
- **Результат:** Статус меняется на "Выполнено", сохраняется дата завершения

#### 1.4 Редактирование задачи
- **Команда:** `/edit <task_id>` - начать редактирование
- **Процесс:**
  1. Бот показывает текущие данные задачи
  2. Запрашивает, что изменить (название/описание)
  3. Пользователь вводит новое значение
  4. Бот обновляет задачу
- **Альтернатива:** Inline-кнопка "✏️ Редактировать" в сообщении с задачей

#### 1.5 Удаление задачи
- **Команда:** `/delete <task_id>` - удалить задачу
- **Альтернатива:** Inline-кнопка "🗑 Удалить" в сообщении с задачей
- **Подтверждение:** Запрос подтверждения перед удалением

### 2. Интерфейс и UX

#### 2.1 Главное меню
При команде `/start` или `/menu` показывается клавиатура:
```
[➕ Создать задачу] [📋 Мои задачи]
[⏳ Активные]      [✅ Выполненные]
[❓ Помощь]
```

#### 2.2 Inline-кнопки для задач
В каждом сообщении с задачей:
- `✅ Выполнено` - изменить статус
- `✏️ Редактировать` - редактировать задачу
- `🗑 Удалить` - удалить задачу
- `📋 Все задачи` - вернуться к списку

#### 2.3 Навигация
- Кнопки пагинации: `⬅️ Назад` и `➡️ Вперед`
- Кнопка "🏠 Главное меню" для возврата

### 3. Команды бота

| Команда | Описание | Пример |
|---------|----------|--------|
| `/start` | Начать работу, показать главное меню | `/start` |
| `/help` | Показать справку по командам | `/help` |
| `/menu` | Показать главное меню | `/menu` |
| `/newtask` | Создать новую задачу | `/newtask` |
| `/tasks` | Показать все задачи | `/tasks` |
| `/active` | Показать активные задачи | `/active` |
| `/done` | Показать завершенные задачи | `/done` |
| `/edit <id>` | Редактировать задачу | `/edit 1` |
| `/delete <id>` | Удалить задачу | `/delete 1` |
| `/done_task <id>` | Отметить задачу выполненной | `/done_task 1` |
| `/cancel` | Отменить текущую операцию | `/cancel` |

### 4. Структура данных

#### 4.1 Таблица: tasks
```sql
CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    title TEXT NOT NULL,
    description TEXT,
    status TEXT NOT NULL DEFAULT 'active',  -- 'active' или 'completed'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_tasks_user_id ON tasks(user_id);
CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_user_status ON tasks(user_id, status);
```

#### 4.2 Модель данных (Python)
```python
@dataclass
class Task:
    id: int
    user_id: int
    title: str
    description: Optional[str]
    status: str  # 'active' | 'completed'
    created_at: datetime
    completed_at: Optional[datetime]
    updated_at: datetime
```

### 5. Обработка ошибок

#### 5.1 Валидация
- Название задачи: минимум 3 символа, максимум 200
- Описание: максимум 1000 символов
- ID задачи: должен существовать и принадлежать пользователю

#### 5.2 Сообщения об ошибках
- "Название задачи слишком короткое (минимум 3 символа)"
- "Задача не найдена"
- "У вас нет прав на эту задачу"
- "Произошла ошибка. Попробуйте позже."

### 6. Форматирование дат

- Формат отображения: `ДД.ММ.ГГГГ ЧЧ:ММ`
- Пример: `25.01.2025 14:30`
- Использовать московское время (UTC+3) или системное время

### 7. Ограничения MVP

**Включается:**
- ✅ Создание, просмотр, редактирование, удаление задач
- ✅ Изменение статуса (активная/выполнена)
- ✅ Фильтрация по статусу
- ✅ Пагинация списков
- ✅ Inline-кнопки для быстрых действий

**НЕ включается (будет добавлено позже):**
- ❌ Проекты/категории
- ❌ Приоритеты
- ❌ Сроки выполнения (deadlines)
- ❌ Напоминания
- ❌ Теги
- ❌ Поиск
- ❌ Экспорт данных
- ❌ AI-агент (будет интегрирован отдельно)

## Технические требования

### Технологии
- Python 3.10+
- python-telegram-bot 20.7+
- SQLite для хранения данных
- python-dotenv для конфигурации

### Производительность
- Отклик на команды: < 1 секунды
- Поддержка до 1000 задач на пользователя
- Оптимизация запросов к БД

### Безопасность
- Валидация всех входных данных
- Проверка прав доступа (пользователь может работать только со своими задачами)
- Защита от SQL injection (параметризованные запросы)
- Ограничение длины текста

## Примеры использования

### Пример 1: Создание задачи
```
User: /newtask
Bot: Введите название задачи:
User: Купить молоко
Bot: Введите описание задачи (или отправьте /skip для пропуска):
User: /skip
Bot: ✅ Задача создана!

📋 Задача #1
Название: Купить молоко
Описание: Без описания
Статус: ⏳ В работе
Создано: 25.01.2025 14:30

[✅ Выполнено] [✏️ Редактировать] [🗑 Удалить]
```

### Пример 2: Просмотр задач
```
User: /tasks
Bot: 📋 Ваши задачи (страница 1/1, всего: 3)

⏳ Задача #1: Купить молоко
   Создано: 25.01.2025 14:30

✅ Задача #2: Подготовить отчет
   Создано: 24.01.2025 10:00

[🏠 Главное меню]
```

### Пример 3: Изменение статуса
```
User: [нажимает кнопку "✅ Выполнено" на задаче #1]
Bot: ✅ Задача "Купить молоко" отмечена как выполненная!

📋 Задача #1
Название: Купить молоко
Статус: ✅ Выполнено
Создано: 25.01.2025 14:30
Завершено: 25.01.2025 15:45
```

## Критерии готовности

1. ✅ Все команды работают корректно
2. ✅ Создание, просмотр, редактирование, удаление задач функционируют
3. ✅ Изменение статуса работает
4. ✅ Фильтрация по статусу работает
5. ✅ Пагинация работает корректно
6. ✅ Inline-кнопки функционируют
7. ✅ Валидация данных работает
8. ✅ Обработка ошибок реализована
9. ✅ База данных стабильна
10. ✅ Бот не падает при некорректных данных

