# Основной функционал Telegram Task Tracker Bot

> **Дата создания:** 2025-01-28  
> **Версия:** 2.0

## Обзор

Telegram Task Tracker Bot - это полнофункциональная система управления проектами и задачами для Telegram, аналог Trello/Jira.

## Основные функции

### 1. Управление пространствами (Workspaces)

Пространства позволяют организовать работу по категориям (Работа, Дом, Спорт и т.д.).

**Основные возможности:**
- Создание пространств с уникальными названиями для каждого пользователя
- Просмотр списка всех пространств пользователя
- Удаление пространств (с каскадным удалением всех данных)
- Переименование пространств

**Команды:**
- `/workspaces` - список пространств
- `/newworkspace <название>` - создать пространство
- `/delworkspace <название>` - удалить пространство
- `/renameworkspace <старое> <новое>` - переименовать пространство

### 2. Управление досками и колонками (Boards & Columns)

Доски - это Kanban-доски с колонками для организации задач.

**Основные возможности:**
- Создание досок с автоматическими дефолтными колонками (Очередь, В работе, Готово)
- Добавление и удаление колонок
- Просмотр досок в текстовом формате (Kanban-визуализация)
- Просмотр досок в виде списка задач по колонкам
- Удаление досок

**Команды:**
- `/boards` - список досок
- `/newboard <название>` - создать доску
- `/delboard <название>` - удалить доску
- `/board <название>` - показать доску (Kanban-визуализация)
- `/boardlist <название>` - показать доску в виде списка
- `/columns <доска>` - список колонок доски
- `/addcolumn <доска> <название>` - добавить колонку
- `/delcolumn <доска> <колонка>` - удалить колонку

### 3. Управление проектами (Projects)

Проекты автоматически создают задачи на всех досках пространства при создании.

**Основные возможности:**
- Создание проектов с настраиваемым ID
- Автоматическое создание задач проекта на всех досках пространства
- Формат названия задачи: "{project_id} {project_name} {board_name}"
- Просмотр проекта с дашбордом и этапами
- Автоматическое определение текущего этапа проекта
- Удаление проектов

**Команды:**
- `/projects` - список проектов
- `/newproject <id> <название>` - создать проект
- `/project <id>` - показать проект
- `/projectdashboard <id>` - показать дашборд проекта
- `/delproject <id>` - удалить проект

**Этапы дашборда:**
- `preparation` - Подготовка
- `design` - Дизайн
- `development` - Разработка
- `testing` - Тестирование
- `submission` - На отправку
- `moderation` - Модерация
- `rejected` - Реджект
- `published` - Опубликовано

### 4. Управление задачами (Tasks)

Задачи - основной элемент системы управления.

**Основные возможности:**
- Создание задач (интерактивный режим)
- Просмотр задач с полной информацией
- Перемещение задач между колонками
- Установка приоритетов (0-3: низкий, средний, высокий, критический)
- Удаление задач
- Просмотр "Мои задачи" (задачи, где пользователь назначен ответственным)
- Просмотр задач с дедлайном на сегодня
- Установка дедлайнов для задач
- Автоматическое треканье дат (started_at, completed_at)

**Команды:**
- `/newtask` - создать задачу (интерактивный режим)
- `/task <id>` - показать задачу
- `/movetask <id> <колонка>` - переместить задачу
- `/priority <id> <0-3>` - установить приоритет
- `/deltask <id>` - удалить задачу
- `/mytasks` - мои задачи
- `/today` - задачи на сегодня
- `/deadline <id> <дата>` - установить дедлайн

**Приоритеты:**
- 🔴 Критический (3)
- 🟠 Высокий (2)
- 🟡 Средний (1)
- 🟢 Низкий (0)

### 5. Синхронизация полей между задачами проекта

При добавлении поля к задаче проекта поле автоматически синхронизируется со всеми задачами проекта.

**Основные возможности:**
- Автоматическая синхронизация полей при добавлении к задаче проекта
- Обновление всех задач проекта при изменении поля
- Управление синхронизацией через таблицу `project_field_sync`

**Особенности:**
- Поля создаются на уровне пространства
- При добавлении поля к задаче проекта поле автоматически добавляется ко всем задачам проекта
- При изменении поля обновляются все задачи проекта

### 6. Приоритеты и метки (Tags)

Организация задач с помощью приоритетов и меток.

**Основные возможности:**
- Установка приоритетов задач (4 уровня)
- Создание меток на уровне пространства
- Добавление меток к задачам
- Фильтрация задач по меткам
- Цветовая кодировка меток

**Команды:**
- `/priority <id> <0-3>` - установить приоритет
- `/newtag <название> [цвет]` - создать метку
- `/addtag <task_id> <tag>` - добавить метку к задаче
- `/deltag <task_id> <tag>` - удалить метку

### 7. Динамические поля (Custom Fields)

Кастомные поля для задач (текст, URL, число, дата, выбор из списка).

**Основные возможности:**
- Создание полей различных типов (text, url, number, date, select)
- Добавление значений полей к задачам
- Валидация значений по типу поля
- Автоматическая синхронизация для задач проекта

**Команды:**
- `/newfield <название> <тип>` - создать поле
- `/addfield <id> <поле> <значение>` - добавить поле к задаче

**Типы полей:**
- `text` - Текст
- `url` - Ссылка (с валидацией)
- `number` - Число
- `date` - Дата
- `select` - Выбор из списка

### 8. Статистика

Статистика по проектам, доскам и задачам.

**Основные возможности:**
- Общая статистика пространства
- Статистика проекта (прогресс, этапы, задачи)
- Статистика доски (распределение задач по колонкам, приоритетам, меткам)

**Команды:**
- `/stats` - общая статистика
- `/statsproject <id>` - статистика проекта
- `/statsboard <название>` - статистика доски

### 9. Визуализация досок

Текстовое представление Kanban-досок.

**Основные возможности:**
- Kanban-визуализация доски (текстовый формат)
- Список задач по колонкам
- Отображение приоритетов, меток, статусов

**Команды:**
- `/board <название>` - показать доску (Kanban)
- `/boardlist <название>` - показать доску (список)

### 10. AI-агенты

Автоматизация работы с задачами через AI.

**Основные возможности:**
- Обработка естественного языка
- Автоматическое выполнение команд
- Интеллектуальная помощь в управлении задачами

**Команды:**
- `/ai <запрос>` - AI-помощник

### 11. Зависимости досок (Board Dependencies)

Автоматическое создание и перемещение задач между досками на основе правил зависимостей.

**Основные возможности:**
- Создание зависимостей между досками
- Автоматическое создание задач при перемещении в определенную колонку
- Автоматическое перемещение задач между досками
- Шаблоны названий задач для зависимостей

**Команды:**
- `/dependencies` - список зависимостей
- `/newdependency <параметры>` - создать зависимость
- `/deldependency <id>` - удалить зависимость

**Типы действий:**
- `create_task` - создать задачу на целевой доске
- `move_task` - переместить задачу на целевую доску

### 12. Назначение задач (Assignees)

Назначение пользователей на задачи и автоматическое добавление участников проекта.

**Основные возможности:**
- Назначение пользователей на задачи
- Автоматическое добавление участников проекта при взятии задачи в работу
- Просмотр задач пользователя
- Определение роли на основе доски

**Особенности:**
- При перемещении задачи в колонку "В работе" пользователь автоматически добавляется как участник проекта
- Роль определяется на основе доски (например, "designer" для доски "Дизайн")

## Архитектура

Система построена по принципу многоуровневой архитектуры:
- **Bot Layer** - обработчики команд и callbacks
- **Business Logic Layer** - сервисы с бизнес-логикой
- **Data Access Layer** - репозитории для работы с БД
- **Database** - SQLite база данных

## Технологии

- Python 3.10+
- python-telegram-bot 20.7+
- SQLite
- python-dotenv

## Версии

- **v2.0** (текущая) - Полный функционал управления проектами
- **v1.0** (устарела) - Базовый таск-трекер

