# Исправленные баги

## Баг #1: Ошибка парсинга HTML в команде /projects

**Описание проблемы:**
Команда `/projects` выдавала ошибку: "Can't parse entities: unsupported start tag "id" at byte offset 93"

**Причина:**
Функция `format_project_list()` возвращает обычный текст без HTML тегов, но в обработчике использовался `parse_mode='HTML'`, что вызывало ошибку парсинга.

**Файл:** `task_tracker_bot/handlers/project.py`, строка 49

**Исправление:**
Убран параметр `parse_mode='HTML'` из вызова `reply_text()` в функции `projects_command()`, так как форматирование не использует HTML теги.

**Статус:** ✅ Исправлено

**Тест для проверки:**
```bash
/projects
```
Команда должна работать без ошибок и выводить список проектов.

---

## Баг #2: Команда /renameworkspace не работает с именами, содержащими пробелы

**Описание проблемы:**
Команда `/renameworkspace Тестовое пространство Тестовое2` не находила пространство "Тестовое пространство", так как брала только первый аргумент (`context.args[0]`) как старое имя.

**Причина:**
Логика парсинга аргументов не учитывала, что старое имя пространства может содержать пробелы.

**Файл:** `task_tracker_bot/handlers/workspace.py`, строки 123-136

**Исправление:**
Добавлена логика поиска старого имени среди существующих пространств пользователя. Алгоритм:
1. Получает список всех пространств пользователя
2. Сортирует их по длине имени (от длинного к короткому)
3. Ищет пространство, имя которого совпадает с началом всех аргументов
4. Если найдено - использует его как старое имя, остальное как новое
5. Если не найдено - использует первый аргумент как старое имя (обратная совместимость)

**Статус:** ✅ Исправлено

**Тест для проверки:**
```bash
/newworkspace Тестовое пространство
/renameworkspace Тестовое пространство Новое имя
```
Команда должна успешно переименовать пространство с пробелами в имени.

---

## Баг #3: HTML форматирование не отображается в команде /start

**Описание проблемы:**
В коде команды `/start` используются HTML теги (`<b>`, `<code>`), но они не отображаются в ответе бота.

**Причина:**
Возможно, это особенность отображения через Telegram MCP или проблема с экранированием символов. Код выглядит корректно - используется `parse_mode='HTML'` и правильные теги.

**Файл:** `task_tracker_bot/handlers/start.py`, строки 8-28

**Статус:** ⚠️ Требует проверки
- Код выглядит корректно
- Возможно, это особенность отображения через MCP
- Необходимо проверить в реальном Telegram клиенте

**Рекомендация:**
Проверить работу команды `/start` в реальном Telegram клиенте. Если проблема подтвердится, возможно, потребуется экранирование специальных символов или использование Markdown вместо HTML.

---

## Баг #4: Команда /delboard не находит доску после удаления пространства

**Описание проблемы:**
После удаления пространства команда `/delboard` не может найти доску, которая была в этом пространстве.

**Причина:**
Это ожидаемое поведение - доски связаны с пространствами, и при удалении пространства доски также должны удаляться (каскадное удаление) или команда должна проверять существование родительского пространства.

**Файл:** `task_tracker_bot/handlers/board.py`

**Статус:** ⚠️ Требует анализа
- Необходимо проверить логику каскадного удаления
- Возможно, это ожидаемое поведение
- Нужно улучшить сообщение об ошибке

**Рекомендация:**
Улучшить сообщение об ошибке, чтобы пользователю было понятно, почему доска не найдена (возможно, она была удалена вместе с пространством).

---

## Итоговая статистика

- ✅ Исправлено: 2 бага
- ⚠️ Требует проверки/анализа: 2 бага
- 📝 Всего найдено: 4 бага

