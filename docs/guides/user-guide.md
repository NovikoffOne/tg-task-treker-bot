# Руководство пользователя

> **Дата создания:** 2025-01-28  
> **Обновлено:** 2025-01-28

Полное руководство по использованию Advanced Telegram Task Tracker Bot.

## Введение

Task Tracker Bot - это продвинутый телеграм-бот для управления проектами и задачами с поддержкой пространств, досок, колонок, проектов и синхронизации данных. Аналог Trello/Jira для Telegram.

## Возможности

- 📁 **Пространства** - организация работы по категориям (Работа/Дом/Спорт)
- 📋 **Доски с колонками** - Kanban-стиль управления задачами
- 📊 **Проекты** - автоматическое создание задач на всех досках при создании проекта
- 🔄 **Синхронизация полей** - автоматическая синхронизация полей между задачами проекта
- 📈 **Дашборд проекта** - отслеживание прогресса с автоматическим определением этапа
- 🏷 **Метки и приоритеты** - организация задач
- 📎 **Динамические поля** - кастомные поля для задач (текст, URL, число, дата)
- 📊 **Статистика** - статистика по проектам, доскам и задачам
- 🎨 **Визуализация досок** - текстовое представление Kanban-досок

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository_url>
cd tg-task-treker-bot
```

2. Установите зависимости:
```bash
pip install -r task_tracker_bot/requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` файл:
```
BOT_TOKEN=your_bot_token_here
DATABASE_PATH=data/tasks.db
```

5. Запустите бота:
```bash
python task_tracker_bot/bot.py
```

## Основные команды

### Пространства

Пространства позволяют организовать работу по категориям (Работа, Дом, Спорт и т.д.).

- `/workspaces` - Список пространств
- `/newworkspace <название>` - Создать пространство
- `/delworkspace <название>` - Удалить пространство
- `/renameworkspace <старое> <новое>` - Переименовать пространство

**Пример:**
```
/newworkspace Работа
/workspaces
```

### Доски

Доски - это Kanban-доски с колонками для организации задач.

- `/boards` - Список досок текущего пространства
- `/newboard <название>` - Создать доску
- `/delboard <название>` - Удалить доску
- `/board <название>` - Показать доску
- `/columns <доска>` - Список колонок доски
- `/addcolumn <доска> <название>` - Добавить колонку
- `/delcolumn <доска> <колонка>` - Удалить колонку

**Пример:**
```
/newboard Дизайн
/board Дизайн
/addcolumn Дизайн В работе
```

### Проекты

Проекты автоматически создают задачи на всех досках пространства.

- `/projects` - Список проектов
- `/newproject <id> <название>` - Создать проект (автоматически создаст задачи на всех досках)
- `/project <id>` - Показать проект
- `/projectdashboard <id>` - Показать дашборд проекта
- `/delproject <id>` - Удалить проект

**Пример:**
```
/newproject 5010 Nano Banana Ai
/project 5010
/projectdashboard 5010
```

**Особенности:**
- При создании проекта автоматически создаются задачи на всех досках пространства
- Формат названия задачи: "{project_id} {project_name} {board_name}"
- Все задачи проекта связаны между собой

### Задачи

- `/newtask` - Создать задачу (интерактивный режим)
- `/task <id>` - Показать задачу
- `/movetask <id> <колонка>` - Переместить задачу
- `/priority <id> <0-3>` - Установить приоритет (0=низкий, 1=средний, 2=высокий, 3=критический)
- `/deltask <id>` - Удалить задачу

**Приоритеты:**
- 🔴 Критический (3)
- 🟠 Высокий (2)
- 🟡 Средний (1)
- 🟢 Низкий (0)

### Поля

Динамические поля для задач (Figma, Git, ТЗ и т.д.).

- `/newfield <название> <тип>` - Создать поле (типы: text, url, number, date, select)
- `/addfield <id> <поле> <значение>` - Добавить поле к задаче (автоматически синхронизируется для проектов)

**Пример:**
```
/newfield figma url
/addfield 1 figma https://figma.com/...
```

**Особенности:**
- При добавлении поля к задаче проекта поле автоматически добавляется ко всем задачам проекта
- При изменении поля обновляются все задачи проекта

### Статистика

- `/stats` - Общая статистика
- `/statsproject <id>` - Статистика проекта
- `/statsboard <название>` - Статистика доски

## Особенности реализации

### Автоматическое создание задач проекта

При создании проекта автоматически создаются задачи на всех досках пространства:
- Формат названия: "{project_id} {project_name} {board_name}"
- Задачи помещаются в первую колонку каждой доски
- Все задачи связываются с проектом

### Синхронизация полей

При добавлении поля к задаче проекта:
- Поле автоматически добавляется ко всем задачам проекта
- При изменении поля обновляются все задачи проекта
- Можно управлять синхронизацией через таблицу `project_field_sync`

### Дашборд проекта

Этап проекта определяется автоматически на основе колонок задач:
- Если задача "Дизайн" в колонке "Готово" → этап "Дизайн" завершен
- Если задача "Разработка" в колонке "В работе" → текущий этап "Разработка"

**Этапы дашборда:**
- `preparation` - Подготовка
- `design` - Дизайн
- `development` - Разработка
- `testing` - Тестирование
- `submission` - На отправку
- `moderation` - Модерация
- `rejected` - Реджект
- `published` - Опубликовано

## Примеры использования

### Пример 1: Создание проекта

```
1. /newworkspace Работа
2. /newboard Дизайн
3. /newboard Разработка
4. /newproject 5010 Nano Banana Ai
5. /project 5010
```

Результат: Создан проект с задачами на досках "Дизайн" и "Разработка".

### Пример 2: Добавление ссылки к проекту

```
1. /newfield figma url
2. /addfield 1 figma https://figma.com/file/...
```

Результат: Ссылка добавлена ко всем задачам проекта.

### Пример 3: Перемещение задачи

```
1. /task 1
2. /movetask 1 В работе
3. /board Дизайн
```

Результат: Задача перемещена в колонку "В работе" на доске "Дизайн".

## Дополнительные ресурсы

- [Навигация по UI](ui-navigation.md) - описание интерфейса
- [Быстрый старт](../getting-started/README.md) - установка и настройка
- [Решение проблем](../getting-started/troubleshooting.md) - частые проблемы

## Технологии

- Python 3.10+
- python-telegram-bot 20.7+
- SQLite
- python-dotenv

