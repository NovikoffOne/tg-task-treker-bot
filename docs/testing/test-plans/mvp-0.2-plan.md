# MVP 0.2 - Детальный план тестирования через Telegram MCP

## 🎯 Цель тестирования

Проверить все функции MVP 0.2 через реальное взаимодействие с ботом через Telegram MCP сервер.

## 📋 Предварительные требования

1. ✅ Бот запущен и работает
2. ✅ Telegram MCP сервер настроен и доступен
3. ✅ Известен username бота (например, `my_task_tracker_bot`)
4. ✅ Есть доступ к Telegram аккаунту через MCP

## 🔧 Настройка тестирования

### Шаг 1: Найти чат с ботом

```python
# Использовать MCP функцию для поиска чата
chat = mcp_telegram-mcp_get_direct_chat_by_contact(contact_query="your_bot_username")
chat_id = chat["id"]
```

### Шаг 2: Отправить /start для инициализации

```python
mcp_telegram-mcp_send_message(chat_id=chat_id, message="/start")
# Подождать 2 секунды
time.sleep(2)
```

---

## 📝 Тест-кейсы

### Группа 1: Базовые команды MVP 0.2

#### Тест 1.1: Команда /mytasks (без задач)
**Цель:** Проверить отображение пустого списка задач

**Шаги:**
1. Отправить `/mytasks`
2. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Бот отвечает сообщением
- В ответе есть слова: "нет", "назначен", "задач" (на русском)
- Нет ошибок

**Проверка:**
```python
response = await get_bot_response(chat_id)
assert any(word in response.lower() for word in ["нет", "назначен", "задач"])
```

---

#### Тест 1.2: Команда /today (без задач)
**Цель:** Проверить отображение пустого списка задач на сегодня

**Шаги:**
1. Отправить `/today`
2. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Бот отвечает сообщением
- В ответе есть слова: "нет", "сегодня", "дедлайн"
- Нет ошибок

**Проверка:**
```python
response = await get_bot_response(chat_id)
assert any(word in response.lower() for word in ["нет", "сегодня", "дедлайн"])
```

---

### Группа 2: Создание структуры для тестирования

#### Тест 2.1: Создание тестового пространства
**Цель:** Создать пространство для тестов

**Шаги:**
1. Отправить `/newworkspace Тестовое пространство MVP02`
2. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Пространство создано успешно
- В ответе есть слова: "создан", "успешно", "пространств"

**Проверка:**
```python
response = await get_bot_response(chat_id)
assert any(word in response.lower() for word in ["создан", "успешно", "пространств"])
```

---

#### Тест 2.2: Создание досок
**Цель:** Создать все необходимые доски

**Шаги:**
1. Отправить `/newboard Подготовка`
2. Подождать ответа (2 сек)
3. Отправить `/newboard Дизайн`
4. Подождать ответа (2 сек)
5. Отправить `/newboard Разработка`
6. Подождать ответа (2 сек)
7. Отправить `/newboard Тестирование`
8. Подождать ответа (2 сек)
9. Отправить `/newboard Подготовка аккаунта`
10. Подождать ответа (2 сек)
11. Отправить `/newboard АСО`
12. Подождать ответа (2 сек)
13. Отправить `/newboard Store`
14. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Все доски созданы успешно
- В каждом ответе есть слова: "создан", "доск"

**Проверка:**
```python
boards = ["Подготовка", "Дизайн", "Разработка", "Тестирование", 
          "Подготовка аккаунта", "АСО", "Store"]
for board in boards:
    response = await send_and_wait(f"/newboard {board}")
    assert any(word in response.lower() for word in ["создан", "доск"])
```

---

#### Тест 2.3: Добавление колонок к доскам
**Цель:** Создать колонки согласно TEST_BASE_CASE.md

**Шаги:**
1. Для доски "Подготовка": добавить колонки (если их нет)
2. Для доски "Дизайн": добавить колонки:
   - `/addcolumn Дизайн Очередь`
   - `/addcolumn Дизайн План на неделю`
   - `/addcolumn Дизайн В работе`
   - `/addcolumn Дизайн На утверждении`
   - `/addcolumn Дизайн Готово`
3. Для доски "Разработка": добавить колонки:
   - `/addcolumn Разработка Очередь`
   - `/addcolumn Разработка План на неделю`
   - `/addcolumn Разработка В работе`
   - `/addcolumn Разработка Тестирование`
   - `/addcolumn Разработка Фикс Багов`
   - `/addcolumn Разработка Готово`
   - `/addcolumn Разработка Отправленно AppConnect`
4. И так далее для остальных досок

**Ожидаемый результат:**
- Все колонки созданы успешно
- В ответе есть слова: "создан", "колонк"

---

#### Тест 2.4: Создание проекта
**Цель:** Создать проект для тестирования зависимостей

**Шаги:**
1. Отправить `/newproject 5001 Pictory Ai`
2. Подождать ответа (3 сек)

**Ожидаемый результат:**
- Проект создан успешно
- Задачи автоматически созданы на всех досках
- В ответе есть слова: "создан", "проект", "5001"

**Проверка:**
```python
response = await send_and_wait("/newproject 5001 Pictory Ai")
assert "5001" in response
assert any(word in response.lower() for word in ["создан", "проект"])
```

---

### Группа 3: Тестирование зависимостей досок

#### Тест 3.1: Создание зависимости "Подготовка -> Дизайн"
**Цель:** Настроить автоматическое создание задачи на доске Дизайн

**Шаги:**
1. Отправить `/newdependency` (или использовать интерактивный режим)
2. Заполнить параметры зависимости:
   - Источник: доска "Подготовка", колонка "Готово"
   - Цель: доска "Дизайн", колонка "Очередь"
   - Действие: создать задачу
   - Шаблон названия: `{project_id} {project_name} Дизайн`
3. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Зависимость создана успешно
- В ответе есть слова: "создан", "зависимость"

---

#### Тест 3.2: Проверка работы зависимости
**Цель:** Проверить автоматическое создание задачи при перемещении

**Шаги:**
1. Найти задачу проекта 5001 на доске "Подготовка"
2. Отправить `/board Подготовка` для просмотра доски
3. Найти ID задачи проекта 5001
4. Отправить `/movetask <task_id> Готово`
5. Подождать ответа (3 сек)
6. Отправить `/board Дизайн` для проверки

**Ожидаемый результат:**
- Задача перемещена в "Готово"
- На доске "Дизайн" автоматически создана задача "5001 Pictory Ai Дизайн"
- Задача находится в колонке "Очередь"

**Проверка:**
```python
# Переместить задачу
response = await send_and_wait(f"/movetask {prep_task_id} Готово")
assert "перемещена" in response.lower() or "успешно" in response.lower()

# Проверить доску Дизайн
response = await send_and_wait("/board Дизайн")
assert "5001" in response
assert "Pictory Ai Дизайн" in response
```

---

#### Тест 3.3: Создание зависимости "Дизайн -> Разработка"
**Цель:** Настроить автоматическое создание задачи на доске Разработка

**Шаги:**
1. Создать зависимость аналогично тесту 3.1
2. Переместить задачу Дизайна в "Готово"
3. Проверить создание задачи на доске Разработка

**Ожидаемый результат:**
- Задача автоматически создана на доске Разработка

---

#### Тест 3.4: Создание зависимости "Разработка -> Тестирование + Подготовка аккаунта"
**Цель:** Проверить создание нескольких задач одновременно

**Шаги:**
1. Создать две зависимости:
   - Разработка "Тестирование" -> Тестирование "Очередь"
   - Разработка "Тестирование" -> Подготовка аккаунта "Очередь"
2. Переместить задачу Разработки в колонку "Тестирование"
3. Проверить создание обеих задач

**Ожидаемый результат:**
- Обе задачи созданы автоматически

---

#### Тест 3.5: Зависимость перемещения "Тестирование Реджект -> Разработка Фикс Багов"
**Цель:** Проверить автоматическое перемещение задачи

**Шаги:**
1. Создать зависимость:
   - Источник: Тестирование "Реджект"
   - Цель: Разработка "Фикс Багов"
   - Действие: переместить задачу
2. Переместить задачу Тестирования в "Реджект"
3. Проверить перемещение задачи Разработки в "Фикс Багов"

**Ожидаемый результат:**
- Задача Разработки автоматически перемещена в "Фикс Багов"

---

### Группа 4: Тестирование участников проекта

#### Тест 4.1: Автоматическое назначение при перемещении
**Цель:** Проверить автоматическое назначение при перемещении в "В работе"

**Шаги:**
1. Найти задачу проекта 5001 на любой доске
2. Отправить `/movetask <task_id> В работе`
3. Подождать ответа (2 сек)
4. Отправить `/mytasks`

**Ожидаемый результат:**
- Задача перемещена успешно
- В списке "Мои задачи" появилась эта задача
- Пользователь автоматически назначен ответственным

**Проверка:**
```python
# Переместить задачу
response = await send_and_wait(f"/movetask {task_id} В работе")
assert "перемещена" in response.lower()

# Проверить мои задачи
response = await send_and_wait("/mytasks")
assert str(task_id) in response
assert "5001" in response
```

---

#### Тест 4.2: Проверка участников проекта
**Цель:** Проверить добавление в участники проекта

**Шаги:**
1. Переместить задачу Дизайна в "План на неделю"
2. Отправить `/project 5001` для просмотра проекта
3. Проверить наличие информации об участниках

**Ожидаемый результат:**
- Пользователь добавлен как участник проекта
- Роль определяется на основе доски (например, "designer" для доски Дизайн)

---

### Группа 5: Тестирование дедлайнов

#### Тест 5.1: Установка дедлайна
**Цель:** Проверить установку дедлайна задачи

**Шаги:**
1. Найти задачу проекта 5001
2. Отправить `/deadline <task_id> 31.12.2025`
3. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Дедлайн установлен успешно
- В ответе есть дата дедлайна

**Проверка:**
```python
response = await send_and_wait(f"/deadline {task_id} 31.12.2025")
assert "дедлайн" in response.lower()
assert "31.12.2025" in response or "31.12" in response
```

---

#### Тест 5.2: Установка дедлайна с временем
**Цель:** Проверить установку дедлайна с указанием времени

**Шаги:**
1. Отправить `/deadline <task_id> 31.12.2025 18:00`
2. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Дедлайн установлен с временем
- В ответе есть время

---

#### Тест 5.3: Проверка команды /today
**Цель:** Проверить отображение задач с дедлайном на сегодня

**Шаги:**
1. Установить дедлайн на сегодня для задачи
2. Отправить `/today`
3. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Задача отображается в списке "Сегодня"
- В ответе есть название задачи

**Проверка:**
```python
from datetime import datetime
today = datetime.now().strftime("%d.%m.%Y")
response = await send_and_wait(f"/deadline {task_id} {today}")
response = await send_and_wait("/today")
assert str(task_id) in response or "5001" in response
```

---

### Группа 6: Тестирование просмотра задачи на доске

#### Тест 6.1: Просмотр доски
**Цель:** Проверить отображение доски с задачами

**Шаги:**
1. Отправить `/board Разработка`
2. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Доска отображается корректно
- Видны задачи с их ID
- Есть inline-кнопки для просмотра задач

---

#### Тест 6.2: Просмотр карточки задачи из доски
**Цель:** Проверить открытие полной информации о задаче

**Шаги:**
1. Отправить `/board Разработка`
2. Найти inline-кнопку с задачей
3. Нажать на кнопку (через callback)
4. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Открывается полная карточка задачи
- Отображаются все поля, метки, участники
- Есть кнопка "Назад к доске"

**Проверка:**
```python
# Открыть доску
response = await send_and_wait("/board Разработка")
# Найти callback_data для задачи (например, "view_task_from_board_123")
# Отправить callback через MCP
# Проверить ответ
```

---

### Группа 7: Тестирование отображения доски в виде списка

#### Тест 7.1: Команда /boardlist
**Цель:** Проверить отображение доски в виде списка

**Шаги:**
1. Отправить `/boardlist Разработка`
2. Подождать ответа (2 сек)

**Ожидаемый результат:**
- Доска отображается в виде списка
- Задачи сгруппированы по колонкам
- Формат: "Колонка:\n- Задача 1\n- Задача 2"

**Проверка:**
```python
response = await send_and_wait("/boardlist Разработка")
assert "Очередь:" in response or "В работе:" in response
assert "5001" in response
```

---

### Группа 8: Тестирование автоматического трекинга дат

#### Тест 8.1: Автоматическая запись started_at
**Цель:** Проверить запись даты начала работы

**Шаги:**
1. Найти задачу в колонке "Очередь"
2. Отправить `/movetask <task_id> В работе`
3. Подождать ответа (2 сек)
4. Отправить `/task <task_id>` для просмотра задачи

**Ожидаемый результат:**
- Задача перемещена успешно
- В карточке задачи есть дата "Начато: [сегодняшняя дата]"

**Проверка:**
```python
from datetime import datetime
today = datetime.now().strftime("%d.%m.%Y")

response = await send_and_wait(f"/movetask {task_id} В работе")
response = await send_and_wait(f"/task {task_id}")
assert "Начато" in response or "начато" in response
assert today in response
```

---

#### Тест 8.2: Автоматическая запись completed_at
**Цель:** Проверить запись даты завершения

**Шаги:**
1. Найти задачу в колонке "В работе"
2. Отправить `/movetask <task_id> Готово`
3. Подождать ответа (2 сек)
4. Отправить `/task <task_id>` для просмотра задачи

**Ожидаемый результат:**
- Задача перемещена успешно
- В карточке задачи есть дата "Завершено: [сегодняшняя дата]"

---

### Группа 9: Комплексное тестирование сценария из TEST_BASE_CASE.md

#### Тест 9.1: Полный сценарий работы с проектом
**Цель:** Проверить весь workflow из TEST_BASE_CASE.md

**Шаги:**
1. Создать все зависимости согласно спецификации
2. Выполнить полный сценарий:
   - Создать проект 5001 Pictory Ai
   - Переместить задачу Подготовки в "Готово"
   - Проверить создание задачи Дизайна
   - Переместить задачу Дизайна в "Готово"
   - Проверить создание задачи Разработки
   - И так далее по всему сценарию

**Ожидаемый результат:**
- Все зависимости работают корректно
- Задачи создаются и перемещаются автоматически
- Все данные синхронизируются

---

## 📊 Отчет о тестировании

После выполнения всех тестов создать отчет:

```markdown
# Отчет о тестировании MVP 0.2

**Дата:** [дата]
**Тестировщик:** [имя]
**Бот:** [username]

## Результаты по группам:

### Группа 1: Базовые команды
- Тест 1.1: ✅/❌
- Тест 1.2: ✅/❌

### Группа 2: Создание структуры
- Тест 2.1: ✅/❌
- Тест 2.2: ✅/❌
- ...

### Итого:
- Всего тестов: X
- Пройдено: Y
- Провалено: Z
- Процент успеха: Y/X * 100%

## Найденные проблемы:
1. [описание проблемы]
2. [описание проблемы]

## Рекомендации:
1. [рекомендация]
2. [рекомендация]
```

---

## 🔧 Вспомогательные функции для тестирования

```python
import asyncio
import time

async def send_and_wait(chat_id: str, message: str, wait_time: float = 2.0) -> str:
    """Отправить сообщение и получить ответ"""
    # Отправить через MCP
    mcp_telegram-mcp_send_message(chat_id=chat_id, message=message)
    await asyncio.sleep(wait_time)
    
    # Получить ответ
    messages = mcp_telegram-mcp_get_messages(chat_id=chat_id, page=1, page_size=5)
    bot_messages = [m for m in messages["messages"] if m["from_id"] != "me"]
    if bot_messages:
        return bot_messages[0]["text"]
    return ""

async def find_task_id(chat_id: str, project_id: str, board_name: str) -> int:
    """Найти ID задачи проекта на доске"""
    response = await send_and_wait(chat_id, f"/board {board_name}")
    # Парсить ID из ответа (например, #123)
    # Вернуть ID
    pass

async def check_keywords(response: str, keywords: list) -> bool:
    """Проверить наличие ключевых слов в ответе"""
    response_lower = response.lower()
    return any(kw.lower() in response_lower for kw in keywords)
```

---

## ✅ Критерии успешного тестирования

Тестирование считается успешным, если:

1. ✅ Все базовые команды работают
2. ✅ Зависимости досок работают корректно
3. ✅ Участники проекта назначаются автоматически
4. ✅ Дедлайны устанавливаются и отображаются
5. ✅ Автоматическое треканье дат работает
6. ✅ Просмотр задачи на доске работает
7. ✅ Отображение доски в виде списка работает
8. ✅ Команды "Мои задачи" и "Сегодня" работают
9. ✅ Полный сценарий из TEST_BASE_CASE.md выполняется без ошибок

---

**Дата создания:** 2025-01-28  
**Версия:** 1.0  
**Статус:** Готов к использованию

