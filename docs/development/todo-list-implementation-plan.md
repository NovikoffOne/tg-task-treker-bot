# План реализации Todo List Feature

> **Дата создания:** 2025-01-29  
> **Версия:** 1.0  
> **Статус:** 📝 Готово к реализации

## 📋 Обзор

Этот документ содержит полный план реализации функционала Todo List с календарным видом и пакетным добавлением задач через AI агентов.

**Связанные документы:**
- [Техническая спецификация](../specifications/todo-list-feature.md)
- [План миграции БД](../architecture/migrations/002_todo_list_migration.md)
- [Руководство по парсингу дат](date-parsing-guide.md)
- [Инструкции для агентов](agent-instructions-todo.md)
- [Тест-план](../testing/todo-list-test-plan.md)

---

## 🎯 Цель

Реализовать функционал туду-листа с:
1. Пакетным добавлением задач через естественный язык
2. Автоматическим определением типа задач (личные/рабочие)
3. Календарным видом с группировкой по датам и времени
4. Парсингом дат и времени из текста

---

## 📝 Этапы реализации

### Этап 1: Миграция базы данных

**Файлы для создания/изменения:**
- `task_tracker_bot/migrations/migrate_todo_list.py` - скрипт миграции

**Задачи:**
1. Создать скрипт миграции на основе `docs/architecture/migrations/002_todo_list_migration.md`
2. Выполнить миграцию для создания таблицы `personal_tasks`
3. Добавить поля `scheduled_date`, `scheduled_time`, `scheduled_time_end` в таблицу `tasks`
4. Создать необходимые индексы
5. Протестировать миграцию

**Проверка:**
- Таблица `personal_tasks` создана
- Поля добавлены в `tasks`
- Индексы созданы

---

### Этап 2: Модели данных

**Файлы для создания:**
- `task_tracker_bot/models/personal_task.py` - модель PersonalTask

**Файлы для изменения:**
- `task_tracker_bot/models/task.py` - добавить поля `scheduled_date`, `scheduled_time`, `scheduled_time_end`

**Задачи:**
1. Создать модель `PersonalTask` с полями из спецификации
2. Добавить методы `from_row`, `time_display`
3. Обновить модель `Task` - добавить новые поля
4. Обновить метод `from_row` в `Task` для поддержки новых полей

**Проверка:**
- Модель `PersonalTask` создана и работает
- Модель `Task` обновлена корректно

---

### Этап 3: Репозитории

**Файлы для создания:**
- `task_tracker_bot/repositories/personal_task_repository.py`

**Файлы для изменения:**
- `task_tracker_bot/repositories/task_repository.py` - добавить поддержку новых полей

**Задачи:**
1. Создать `PersonalTaskRepository` с методами:
   - `create()` - создание личной задачи
   - `get_by_date()` - получение задач на дату
   - `get_by_date_range()` - получение задач в диапазоне
   - `mark_completed()` - отметка как выполненной
   - `get_by_id()` - получение по ID
   - `update()` - обновление задачи
   - `delete()` - удаление задачи
2. Обновить `TaskRepository.create()` - добавить параметры `scheduled_date`, `scheduled_time`, `scheduled_time_end`
3. Обновить `TaskRepository.update()` - добавить поддержку новых полей
4. Добавить метод `get_by_scheduled_date()` в `TaskRepository`

**Проверка:**
- Все методы репозиториев работают корректно
- Поддержка новых полей реализована

---

### Этап 4: Утилиты

**Файлы для создания:**
- `task_tracker_bot/utils/date_parser.py` - парсер дат и времени
- `task_tracker_bot/utils/task_classifier.py` - классификатор задач

**Задачи:**

#### DateParser (`date_parser.py`):
1. Реализовать метод `parse_date()` - парсинг относительных и абсолютных дат
2. Реализовать метод `parse_time()` - парсинг времени HH:MM
3. Реализовать метод `parse_time_range()` - парсинг диапазона времени
4. Реализовать метод `parse_datetime_from_task()` - извлечение даты/времени из текста
5. Добавить обработку ошибок и валидацию
6. Использовать библиотеку `python-dateutil` для сложных случаев

#### TaskClassifier (`task_classifier.py`):
1. Реализовать метод `classify_task()` - определение типа задачи
2. Добавить паттерны для извлечения project_id
3. Реализовать проверку существования проекта
4. Реализовать очистку заголовка от project_id

**Проверка:**
- Парсер корректно обрабатывает все форматы дат
- Классификатор правильно определяет тип задач

---

### Этап 5: Сервисы

**Файлы для создания:**
- `task_tracker_bot/services/todo_service.py`

**Задачи:**
1. Создать класс `TodoService` с зависимостями:
   - `PersonalTaskRepository`
   - `TaskRepository`
   - `ProjectRepository`
   - `DateParser`
   - `TaskClassifier`
   - `ColumnRepository` (для создания рабочих задач)
2. Реализовать метод `create_todo_batch()`:
   - Парсинг текста с задачами
   - Разбиение на отдельные задачи
   - Определение типа каждой задачи
   - Создание личных задач в `personal_tasks`
   - Создание рабочих задач в `tasks` с `project_id`
   - Обработка множественного времени
   - Возврат результата с созданными задачами
3. Реализовать метод `get_todo_list()`:
   - Получение личных задач на дату
   - Получение рабочих задач на дату
   - Группировка по времени
   - Форматирование для отображения
4. Реализовать метод `mark_personal_task_completed()`

**Проверка:**
- Сервис корректно создает пакеты задач
- Правильно определяет тип задач
- Корректно группирует задачи по времени

---

### Этап 6: Обновление AI агентов

**Файлы для изменения:**
- `task_tracker_bot/agents/orchestrator.py` - обновить system prompt
- `task_tracker_bot/agents/task_manager.py` - добавить метод `create_todo_batch`
- `task_tracker_bot/agents/data_manager.py` - добавить методы для `personal_tasks`

**Задачи:**

#### OrchestratorAgent:
1. Обновить `get_system_prompt()` - добавить описание intent `add_todo_batch`
2. Добавить логику распознавания запросов на пакетное добавление
3. Добавить извлечение даты из запроса ("на завтра", "на 03.12")

#### TaskManagerAgent:
1. Обновить `get_system_prompt()` - добавить описание Todo List функционала
2. Добавить метод `create_todo_batch()` - использовать `TodoService`
3. Интегрировать с `DateParser` и `TaskClassifier`

#### DataManagerAgent:
1. Обновить `get_system_prompt()` - добавить описание работы с `personal_tasks`
2. Добавить метод `create_personal_task()`
3. Добавить метод `get_personal_tasks_by_date()`
4. Добавить метод `mark_personal_task_completed()`
5. Обновить метод `create_task()` - добавить поддержку `scheduled_date`, `scheduled_time`

**Проверка:**
- Агенты корректно обрабатывают запросы на пакетное добавление
- Правильно определяют тип задач
- Создают задачи в нужных таблицах

---

### Этап 7: Handlers

**Файлы для создания:**
- `task_tracker_bot/handlers/todo_handler.py`

**Файлы для изменения:**
- `task_tracker_bot/handlers/ai_handler.py` - интеграция с туду-листом

**Задачи:**
1. Создать `todo_handler.py` с функциями:
   - `todo_command()` - команда `/todo [дата]`
   - `handle_todo_date_callback()` - обработка выбора даты
   - `handle_todo_navigation()` - навигация по датам (вчера/сегодня/завтра)
   - `handle_mark_todo_completed()` - отметка задачи как выполненной
2. Обновить `ai_handler.py`:
   - Добавить обработку intent `add_todo_batch`
   - Интегрировать с `TodoService`

**Проверка:**
- Команда `/todo` работает корректно
- Навигация по датам функционирует
- AI корректно обрабатывает пакетное добавление

---

### Этап 8: UI компоненты

**Файлы для изменения:**
- `task_tracker_bot/utils/keyboards.py` - добавить клавиатуры для туду-листа
- `task_tracker_bot/utils/formatters.py` - добавить форматирование туду-листа
- `task_tracker_bot/handlers/menu_buttons.py` - добавить кнопку "📅 Туду-лист"

**Задачи:**
1. Добавить функцию `todo_list_keyboard()` в `keyboards.py`:
   - Кнопки навигации: "◀️ Вчера", "📅 Сегодня", "Завтра ▶️"
   - Кнопки для отметки задач как выполненных
2. Добавить функцию `format_todo_list()` в `formatters.py`:
   - Форматирование с группировкой по времени
   - Отдельные секции для личных и рабочих задач
   - Отображение времени выполнения
3. Добавить обработку кнопки "📅 Туду-лист" в `menu_buttons.py`
4. Обновить `main_menu_keyboard()` - добавить кнопку туду-листа

**Проверка:**
- Клавиатуры отображаются корректно
- Форматирование туду-листа читаемое
- Навигация работает

---

### Этап 9: Интеграция в bot.py

**Файлы для изменения:**
- `task_tracker_bot/bot.py`

**Задачи:**
1. Импортировать новые handlers
2. Добавить обработчик команды `/todo`
3. Добавить callback handlers для туду-листа
4. Обновить обработку AI запросов для поддержки `add_todo_batch`

**Проверка:**
- Все команды зарегистрированы
- Callbacks работают корректно

---

### Этап 10: Тестирование

**Файлы для создания:**
- `task_tracker_bot/tests/test_date_parser.py`
- `task_tracker_bot/tests/test_task_classifier.py`
- `task_tracker_bot/tests/test_todo_service.py`
- `task_tracker_bot/tests/test_todo_integration.py`
- `task_tracker_bot/tests/test_todo_migration.py`

**Задачи:**
1. Написать unit тесты для `DateParser`
2. Написать unit тесты для `TaskClassifier`
3. Написать unit тесты для `TodoService`
4. Написать интеграционные тесты
5. Написать тесты миграции БД
6. Запустить все тесты и исправить ошибки

**Проверка:**
- Все тесты проходят
- Покрытие кода тестами достаточное

---

## 📚 Порядок выполнения

**ВАЖНО:** Выполнять этапы строго последовательно!

1. ✅ Этап 1: Миграция БД (основа для всего)
2. ✅ Этап 2: Модели данных (нужны для репозиториев)
3. ✅ Этап 3: Репозитории (нужны для сервисов)
4. ✅ Этап 4: Утилиты (нужны для сервисов)
5. ✅ Этап 5: Сервисы (нужны для handlers)
6. ✅ Этап 6: AI агенты (можно параллельно с этапом 7)
7. ✅ Этап 7: Handlers (нужны для bot.py)
8. ✅ Этап 8: UI компоненты (можно параллельно с этапом 7)
9. ✅ Этап 9: Интеграция в bot.py (финальная интеграция)
10. ✅ Этап 10: Тестирование (после всех этапов)

---

## 🔍 Чеклист готовности

Перед началом реализации убедитесь:

- [ ] Изучены все связанные документы
- [ ] Понятна архитектура системы
- [ ] Понятны требования к функционалу
- [ ] Готова тестовая среда

---

## 📖 Справочная информация

### Ключевые файлы документации:

1. **Техническая спецификация:** `docs/specifications/todo-list-feature.md`
   - Полное описание функционала
   - API методов
   - Примеры использования

2. **План миграции:** `docs/architecture/migrations/002_todo_list_migration.md`
   - SQL скрипты
   - Python скрипт миграции
   - Инструкции по откату

3. **Руководство по парсингу:** `docs/development/date-parsing-guide.md`
   - Поддерживаемые форматы
   - Примеры кода
   - Обработка ошибок

4. **Инструкции для агентов:** `docs/development/agent-instructions-todo.md`
   - Обновленные промпты
   - Примеры запросов/ответов
   - Формат JSON

5. **Тест-план:** `docs/testing/todo-list-test-plan.md`
   - Unit тесты
   - Интеграционные тесты
   - E2E тесты

---

## ⚠️ Важные замечания

1. **Миграция БД:** Обязательно выполнить первой, перед созданием моделей
2. **Зависимости:** Установить `python-dateutil` для парсинга дат
3. **Тестирование:** Писать тесты параллельно с разработкой
4. **Обработка ошибок:** Все методы должны иметь обработку ошибок
5. **Валидация:** Валидировать все входные данные

---

## 🎯 Критерии успешной реализации

Функционал считается реализованным, когда:

- [ ] Миграция БД выполнена успешно
- [ ] Все модели и репозитории созданы
- [ ] Парсер дат работает со всеми форматами
- [ ] Классификатор правильно определяет тип задач
- [ ] Сервис создает пакеты задач корректно
- [ ] AI агенты обрабатывают запросы на пакетное добавление
- [ ] Команда `/todo` отображает задачи правильно
- [ ] Навигация по датам работает
- [ ] Все тесты проходят
- [ ] Документация обновлена

---

**Дата создания:** 2025-01-29  
**Готово к использованию в режиме Plan**

